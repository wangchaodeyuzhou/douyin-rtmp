# FFmpeg 推流功能使用说明

## 功能概述

新增的 FFmpeg 推流功能允许用户选择本地视频文件进行自动循环推流。该功能集成在主界面的右侧"推流管理"面板中。

## 主要特性

### 🎯 核心功能
- **视频文件选择**：支持选择本地视频文件（MP4, AVI, MKV, MOV, FLV, WMV, TS, M4V 等格式）
- **自动推流**：使用 FFmpeg 进行高质量视频推流
- **循环推流**：视频播放完成后自动循环重新开始
- **实时日志**：显示详细的推流状态和 FFmpeg 输出日志
- **智能验证**：自动检查视频文件有效性和推流地址格式

### 🛡️ 安全特性
- **文件格式验证**：自动检查视频文件格式和完整性
- **推流地址验证**：验证 RTMP 地址和推流码的有效性
- **FFmpeg 状态检测**：实时检测 FFmpeg 安装状态
- **进程管理**：安全启动和停止推流进程

## 使用步骤

### 1. 准备工作
确保系统已安装 FFmpeg：
- 下载地址：https://ffmpeg.org/download.html
- 将 FFmpeg 添加到系统环境变量
- 在推流面板点击"检查FFmpeg"按钮验证安装

### 2. 获取推流信息
使用左侧的"控制面板"获取推流服务器地址和推流码：
1. 选择网络接口
2. 点击"开始捕获"
3. 在直播软件中开始推流，程序会自动捕获推流信息

### 3. 选择视频文件
在"推流管理"面板中：
1. 点击"选择"按钮
2. 浏览并选择要推流的视频文件
3. 程序会自动验证文件格式和完整性

### 4. 开始推流
1. 确保已获取有效的推流服务器地址和推流码
2. 确保已选择有效的视频文件
3. 点击"开始推流"按钮
4. 观察推流日志区域的实时输出

### 5. 停止推流
- 点击"停止推流"按钮即可安全停止推流
- 程序会自动清理相关进程

## 界面说明

### 推流管理面板布局
```
┌─────────────────────────────────┐
│ FFmpeg: 可用    推流状态: 未推流 │
├─────────────────────────────────┤
│ 视频文件: [选择文件路径]  [选择]  │
├─────────────────────────────────┤
│ [开始推流]  [停止推流]          │
├─────────────────────────────────┤
│ [检查FFmpeg]  [清除日志]        │
├─────────────────────────────────┤
│          推流日志区域            │
│                                 │
│   实时显示 FFmpeg 输出日志       │
│                                 │
└─────────────────────────────────┘
```

## 技术规格

### 支持的视频格式
- MP4 (推荐)
- AVI
- MKV  
- MOV
- FLV
- WMV
- TS
- M4V

### FFmpeg 推流参数
```bash
ffmpeg -re -stream_loop -1 -i <视频文件> \
  -c:v libx264 -preset fast \
  -maxrate 3000k -bufsize 6000k \
  -pix_fmt yuv420p -g 50 \
  -c:a aac -b:a 128k -ac 2 -ar 44100 \
  -f flv <推流地址>
```

### 性能优化
- **编码预设**：fast（平衡质量和性能）
- **最大码率**：3000k（适合大多数直播平台）
- **缓冲区大小**：6000k（保证推流稳定性）
- **音频码率**：128k（高质量音频）

## 常见问题

### Q: 提示"FFmpeg 不可用"怎么办？
**A:** 
1. 确保已下载并安装 FFmpeg
2. 将 FFmpeg 的 bin 目录添加到系统环境变量 PATH 中
3. 重启程序并点击"检查FFmpeg"按钮

### Q: 视频文件检查失败怎么办？
**A:** 
1. 确保文件格式在支持列表中
2. 检查文件是否损坏或为空
3. 尝试使用其他播放器验证文件可正常播放

### Q: 推流失败怎么办？
**A:** 
1. 确保推流地址和推流码正确
2. 检查网络连接
3. 查看推流日志区域的错误信息
4. 尝试使用其他推流软件验证推流地址有效性

### Q: 如何提高推流质量？
**A:** 
1. 使用高质量的源视频文件
2. 确保网络带宽充足（建议 > 5Mbps）
3. 选择较小分辨率的视频文件可提高稳定性

## 注意事项

### ⚠️ 重要提醒
1. **版权合规**：请确保推流的视频内容符合版权法规
2. **平台规则**：遵守直播平台的服务条款和社区准则
3. **网络带宽**：推流需要稳定的上行带宽
4. **系统资源**：长时间推流会消耗 CPU 和内存资源

### 💡 使用建议
- 推荐使用 H.264 编码的 MP4 文件
- 视频分辨率建议在 1080p 以下
- 定期清理推流日志以节省内存
- 推流前测试网络连接稳定性

## 更新日志

### v1.0.0 (2024-09-01)
- ✅ 新增 FFmpeg 推流功能
- ✅ 支持多种视频格式
- ✅ 自动循环推流
- ✅ 实时推流日志
- ✅ 智能文件验证
- ✅ 安全进程管理

---

如有问题或建议，请联系开发团队或提交 Issue。